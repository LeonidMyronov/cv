import { SectionTitleDirective } from './section-title.directive';

describe('SectionTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionTitleDirective();
    expect(directive).toBeTruthy();
  });
});
